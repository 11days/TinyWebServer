webbench (1.5) unstable; urgency=low

  * allow building with both Gnu and BSD make  
  // 允许在GNU和BSD make下构建

 -- Radim Kolar <hsn@home>  Fri, Jun 25 12:00:20 CEST 2004

webbench (1.4) unstable; urgency=low

  * check if url is not too long  
  // 检查URL是否过长
  * report correct program version number  
  // 报告正确的程序版本号
  * use yield() when waiting for test start  
  // 在等待测试开始时使用yield()
  * corrected error codes  
  // 更正了错误代码
  * check availability of test server first  
  // 首先检查测试服务器的可用性
  * do not abort test if first request failed  
  // 如果第一个请求失败，则不中止测试
  * report when some childrens are dead.  
  // 报告有子进程死亡时
  * use alarm, not time() for lower syscal use by bench  
  // 使用alarm而不是time()以减少bench的系统调用
  * use mode 644 for installed doc  
  // 为已安装的文档使用644模式
  * makefile cleaned for better freebsd ports integration  
  // 清理makefile以更好地集成到FreeBSD端口

 -- Radim Kolar <hsn@home>  Thu, 15 Jan 2004 11:15:52 +0100

webbench (1.3) unstable; urgency=low

  * Build fixes for freeBSD  // 修复了FreeBSD的构建问题
  * Default benchmark time 60 -> 30  // 默认基准时间由60更改为30
  * generate tar with subdirectory  // 生成带有子目录的tar文件
  * added to freeBSD ports collection  // 添加到FreeBSD端口集合

 -- Radim Kolar <hsn@home>  Mon, 12 Jan 2004 17:00:24 +0100

webbench (1.2) unstable; urgency=low

  * Only debian-related bugfixes  // 仅针对Debian相关的错误进行修复
  * Updated Debian/rules  // 更新了Debian/rules
  * Adapted to fit new directory system  // 适应新的目录系统
  * moved from debstd to dh_*  // 从debstd移至dh_*

 -- Radim Kolar <hsn@home>  Fri, 18 Jan 2002 12:33:04 +0100

webbench (1.1) unstable; urgency=medium
 
  * Program debianized  // Debian化程序
  * added support for multiple methods (GET, HEAD, OPTIONS, TRACE)  // 添加了对多种方法（GET、HEAD、OPTIONS、TRACE）的支持
  * added support for multiple HTTP versions (0.9 -- 1.1)  // 添加了对多个HTTP版本（0.9至1.1）的支持
  * added long options  // 添加了长选项
  * added multiple clients  // 添加了多个客户端
  * wait for start of second before test  // 在测试之前等待第二秒开始
  * test time can be specified  // 可以指定测试时间
  * better error checking when reading reply from server  // 从服务器读取回复时进行更好的错误检查
  * FIX: tests was one second longer than expected  // 修复：测试时间比预期长一秒

 -- Radim Kolar <hsn@home>  Thu, 16 Sep 1999 18:48:00 +0200

Local variables:
mode: debian-changelog
End:

